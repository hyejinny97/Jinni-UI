import { Meta, Canvas, ArgTypes, Stories } from '@storybook/blocks';
import * as ButtonGroupStories from './ButtonGroup.stories';

<Meta of={ButtonGroupStories} />

# ButtonGroup

연관된 버튼들의 모음

## Import

```js
import { ButtonGroup } from '@/components/general/ButtonGroup';
```

## Usage

### Basic ButtonGroup

- `children`: `Button` 컴포넌트들

<Canvas of={ButtonGroupStories.BasicButtonGroup} />

### Orientation

- `orientation`: 버튼 방향

<Canvas of={ButtonGroupStories.Orientation} />

### Full Width

- `fullWidth`: true이면, 버튼 그룹 너비는 parent의 너비와 동일하게 맞춰짐

<Canvas of={ButtonGroupStories.FullWidth} />

### Button props

`ButtonGroup` 컴포넌트에 적용된 일부 Button props는 하위 Button들에 일괄 적용됨

#### 1. Variant

<Canvas of={ButtonGroupStories.Variant} />

#### 2. Color

<Canvas of={ButtonGroupStories.Color} />

#### 3. Size

<Canvas of={ButtonGroupStories.Size} />

#### 4. Overlay effect

<Canvas of={ButtonGroupStories.OverlayColor} />

#### 5. Ripple effect

<Canvas of={ButtonGroupStories.RippleStartLocation} />

#### 6. Elevation effect

<Canvas of={ButtonGroupStories.Elevation} />

#### 7. Disabled

<Canvas of={ButtonGroupStories.Disabled} />

### Split Button

<Canvas of={ButtonGroupStories.SplitButton} />

## Props

<ArgTypes of={ButtonGroupStories} sort="alpha" />
