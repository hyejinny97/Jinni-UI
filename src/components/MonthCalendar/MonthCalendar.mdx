import { Meta, Canvas, ArgTypes, Stories } from '@storybook/blocks';
import * as MonthCalendarStories from './MonthCalendar.stories';

<Meta of={MonthCalendarStories} />

# MonthCalendar

month 달력

## Import

```js
import MonthCalendar from '@/components/MonthCalendar';
```

## Usage

### Basic MonthCalendar

- `displayedDate`: 화면에 보여지는 날짜

<Canvas of={MonthCalendarStories.BasicMonthCalendar} />

### Selected date

- `selectedDate`: 선택된 날짜
- `onMonthChange`: month button을 클릭했을 때 호출되는 함수

<Canvas of={MonthCalendarStories.SelectedDate} />

### Locale

- `locale`: BCP47 언어 태그를 포함하는 문자열

<Canvas of={MonthCalendarStories.Locale} />

### Restrict Date

- `disabledDates`: 비활성화 하는 특정 날짜 모음

<Canvas of={MonthCalendarStories.DisabledDates} />

### ReadOnly

- `readOnly`: true이면, 수정 불가

<Canvas of={MonthCalendarStories.ReadOnly} />

### Disabled

- `disabled`: true이면, 비활성화됨

<Canvas of={MonthCalendarStories.Disabled} />

### Custom Month

- `renderMonth`: monthProps를 인자로 받아 `Month` 컴포넌트를 반환하는 함수

<Canvas of={MonthCalendarStories.CustomMonth} />

## Props

<ArgTypes of={MonthCalendarStories} sort="alpha" />
