import { Meta, Canvas, ArgTypes, Stories } from '@storybook/blocks';
import * as PaginationItemStories from './PaginationItem.stories';

<Meta of={PaginationItemStories} />

# PaginationItem

pagination item

## Import

```js
import { PaginationItem } from '@/components/navigation/Pagination';
```

## Usage

### Type

- `type`: pagination item 타입
- `page`: 특정 pagination item에 해당하는 페이지

<Canvas of={PaginationItemStories.Type} />

### Button props

- 내부적으로 `Button` 컴포넌트를 반환하므로, `Button` 의 모든 props 사용 가능

#### 1. Variant

<Canvas of={PaginationItemStories.Variant} />

#### 2. Disabled

<Canvas of={PaginationItemStories.Disabled} />

#### 3. Overlay effect

<Canvas of={PaginationItemStories.OverlayEffect} />

#### 4. Ripple effect

<Canvas of={PaginationItemStories.RippleEffect} />

#### 5. Elevation effect

<Canvas of={PaginationItemStories.ElevationEffect} />

### Selected

- `selected`: selected page인지 여부

<Canvas of={PaginationItemStories.Selected} />

### Color

- `color`: selected page 색상

<Canvas of={PaginationItemStories.Color} />

### Shape

- `shape`: pagination item 모양

<Canvas of={PaginationItemStories.Shape} />

### Size

- `size`: pagination item 크기

<Canvas of={PaginationItemStories.Size} />

## Props

<ArgTypes of={PaginationItemStories} sort="alpha" />
