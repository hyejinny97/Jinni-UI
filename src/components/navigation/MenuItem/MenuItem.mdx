import { Meta, Canvas, ArgTypes, Stories } from '@storybook/blocks';
import * as MenuItemStories from './MenuItem.stories';

<Meta of={MenuItemStories} />

# MenuItem

메뉴의 아이템

## Import

```js
import { MenuItem } from '@/components/navigation/MenuItem';
```

## Usage

### Basic MenuItem

- `children`: 메뉴 아이템 콘텐츠

<Canvas of={MenuItemStories.BasicMenuItem} />

### Selected

- `selected`: true이면, 선택됨

<Canvas of={MenuItemStories.Selected} />

### ButtonBase props

- 내부적으로 `ButtonBase` 컴포넌트를 반환하므로, `ButtonBase`의 모든 props 사용 가능

#### 1) Link MenuItem

<Canvas of={MenuItemStories.LinkMenuItem} />

#### 2) Overlay effect

<Canvas of={MenuItemStories.OverlayEffect} />

#### 3) Ripple effect

1. Ripple start location

<Canvas of={MenuItemStories.RippleStartLocation} />

2. Disable overlay effect

<Canvas of={MenuItemStories.DisableRipple} />

#### 4) Disabled

<Canvas of={MenuItemStories.Disabled} />

## Props

<ArgTypes of={MenuItemStories} sort="alpha" />
