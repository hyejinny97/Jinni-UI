import { Meta, Canvas, ArgTypes, Stories, Markdown } from '@storybook/blocks';
import * as CalendarHeaderStories from './CalendarHeader.stories';

<Meta of={CalendarHeaderStories} />

# CalendarHeader

calendar header

## Import

```js
import { CalendarHeader } from '@/components/data-entry/CalendarHeader';
```

## Usage

### Basic CalendarHeader

- `children`: Header 중앙에 위치할 콘텐츠

<Canvas of={CalendarHeaderStories.BasicCalendarHeader} />

### Handle control buttons clicked

- `onPrevClick`: prev button 클릭 시 호출되는 함수
- `onNextClick`: next button 클릭 시 호출되는 함수

<Canvas of={CalendarHeaderStories.HandleControlButtonsClicked} />

### Hide control buttons

- `hidePrevButton`: true이면, prev button을 숨김
- `hideNextButton`: true이면, next button을 숨김

<Canvas of={CalendarHeaderStories.HideControlButtons} />

### Custom control buttons icon

- `prevIcon`: prev button의 icon
- `nextIcon`: next button의 icon

<Canvas of={CalendarHeaderStories.CustomControlButtonsIcon} />

### Custom CalendarHeader

<Canvas of={CalendarHeaderStories.CustomCalendarHeader} />

## Props

<ArgTypes of={CalendarHeaderStories} sort="alpha" />
