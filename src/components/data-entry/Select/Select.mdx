import { Meta, Canvas, ArgTypes, Stories } from '@storybook/blocks';
import * as SelectStories from './Select.stories';
import * as OptionStories from './Option/Option.stories';

<Meta of={SelectStories} />

# Select

options 목록에서 하나 선택 가능한 필드

## Import

```js
import { Select, Option } from '@/components/data-entry/Select';
```

## Usage

### Basic Select

- `defaultValue`: 초기 selected value

<Canvas of={SelectStories.BasicSelect} />

### Controlled Select

- `value` prop을 통해 select를 control할 수 있음
- `value`: selected value
- `onChange`: selected value가 변경됐을 때 호출되는 함수

<Canvas of={SelectStories.ControlledSelect} />

### Placeholder

- `placeholder`: input의 placeholder

<Canvas of={SelectStories.Placeholder} />

### Disabled

- `disabled`: true이면, 비활성화됨

<Canvas of={SelectStories.Disabled} />

### Multiple Select

- `multiple`: true이면, multiple selections이 가능함

<Canvas of={SelectStories.MultipleSelect} />

### Group Option

<Canvas of={SelectStories.GroupOption} />

### Rendering values in chip

- `renderValue`: selected option의 value와 label을 입력값으로 받아, input 내부 content를 반환하는 함수

<Canvas of={SelectStories.RenderingValuesInChip} />

### Option with Checkbox

<Canvas of={SelectStories.OptionWithCheckbox} />

### Select with form

<Canvas of={SelectStories.SelectWithForm} />

### InputBase Props

- `Select` 컴포넌트는 내부적으로 `InputBase` 컴포넌트를 반환하므로, `InputBase` 의 모든 props 사용 가능

#### 1. Variants

<Canvas of={SelectStories.Variants} />

#### 2. Sizes

<Canvas of={SelectStories.Sizes} />

#### 3. Color

<Canvas of={SelectStories.Color} />

#### 4. Full Width

<Canvas of={SelectStories.FullWidth} />

#### 5. Adornments

<Canvas of={SelectStories.Adornments} />

#### 6. Disable Effects

<Canvas of={SelectStories.DisableEffects} />

### Customize Menu

- `Select` 컴포넌트는 내부적으로 `Menu` 컴포넌트를 반환하므로, `MenuProps`를 통해 `Menu`를 커스텀 가능
- `MenuProps`: Menu 컴포넌트의 props

#### 1. Dense

<Canvas of={SelectStories.Dense} />

#### 2. Menu Position

<Canvas of={SelectStories.MenuPosition} />

## Props

### Select

- `InputBase` 컴포넌트 + `Menu` 컴포넌트로 구성됨

<ArgTypes of={SelectStories} sort="alpha" />

### Option

- `MenuItem` 컴포넌트로 구성됨 (`MenuItem` 컴포넌트의 props 전부 사용 가능)

<ArgTypes of={OptionStories} sort="alpha" />
