import { FormControlLabel } from '@/components/_share/FormControlLabel';
import { CheckboxProps } from '@/components/data-entry/Checkbox';

const CheckboxLabel = FormControlLabel<CheckboxProps>;

export default CheckboxLabel;
