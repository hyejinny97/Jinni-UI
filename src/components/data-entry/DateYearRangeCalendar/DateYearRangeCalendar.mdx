import { Meta, Canvas, ArgTypes, Stories } from '@storybook/blocks';
import * as DateYearRangeCalendarStories from './DateYearRangeCalendar.stories';

<Meta of={DateYearRangeCalendarStories} />

# DateYearRangeCalendar

연도별 달력

## Import

```js
import { DateYearRangeCalendar } from '@/components/data-entry/DateRangeCalendar';
```

## Usage

### Basic DateYearRangeCalendar

- `defaultValue`: 초기 selected date

<Canvas of={DateYearRangeCalendarStories.BasicDateYearRangeCalendar} />

### Controlled DateYearRangeCalendar

- `value`를 통해 control 가능
- `value`: selected date
- `onChange`: value가 변경됐을 때 호출되는 함수

<Canvas of={DateYearRangeCalendarStories.ControlledDateYearRangeCalendar} />

### Choose the initial date

- `referenceDate`: `value`, `defaultValue` prop이 없을 때 참조하는 date

<Canvas of={DateYearRangeCalendarStories.ChooseTheInitialDate} />

### Locale and Options

#### 1. Locale

- `locale`: BCP47 언어 태그를 포함하는 문자열

<Canvas of={DateYearRangeCalendarStories.Locale} />

#### 2. Options

- `options`: date 속성

<Canvas of={DateYearRangeCalendarStories.Options} />

### Restrict Date

#### 1. Min date

- `minDate`: 선택 가능한 최소 날짜

<Canvas of={DateYearRangeCalendarStories.MinDate} />

#### 2. Max date

- `maxDate`: 선택 가능한 최대 날짜

<Canvas of={DateYearRangeCalendarStories.MaxDate} />

### ReadOnly

- `readOnly`: true이면, 수정 불가

<Canvas of={DateYearRangeCalendarStories.ReadOnly} />

### Disabled

- `disabled`: true이면, 비활성화됨

<Canvas of={DateYearRangeCalendarStories.Disabled} />

### YearRangeCalendar props

- `DateYearRangeCalendar` 컴포넌트는 내부적으로 `YearRangeCalendar` 컴포넌트를 반환하므로, `YearRangeCalendar` 의 모든 props 사용 가능

#### 1. Order of years

<Canvas of={DateYearRangeCalendarStories.OrderOfYears} />

#### 2. Custom RangeYear

<Canvas of={DateYearRangeCalendarStories.CustomRangeYear} />

### Custom CalendarHeader

- `renderCalendarHeader`: calendarHeaderProps를 인자로 받아 `CalendarHeader` 컴포넌트를 반환하는 함수

<Canvas of={DateYearRangeCalendarStories.CustomCalendarHeader} />

## Props

<ArgTypes of={DateYearRangeCalendarStories} sort="alpha" />
