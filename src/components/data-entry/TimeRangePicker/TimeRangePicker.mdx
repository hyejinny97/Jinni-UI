import { Meta, Canvas, ArgTypes, Stories } from '@storybook/blocks';
import * as TimeRangePickerStories from './TimeRangePicker.stories';

<Meta of={TimeRangePickerStories} />

# TimeRangePicker

시간 범위 선택 가능

## Import

```js
import { TimeRangePicker } from '@/components/data-entry/TimeRangePicker';
```

## Usage

### Basic TimeRangePicker

#### 1. Mode

- `mode`: time 선택 방법
  - `'preset'`: '12:00', '12:30', '01:00' 처럼 미리 정의된 시간 목록에서 하나를 선택하는 방식
  - `'manual'`: 시(hour), 분(minute), 초(second)를 각각 선택하는 방식

<Canvas of={TimeRangePickerStories.Mode} />

#### 2. Default value

- `defaultValue`: 초기 time (uncontrolled일 때만 적용 가능)

<Canvas of={TimeRangePickerStories.DefaultValue} />

### Controlled TimeRangePicker

- `value`를 통해 control 가능
- `value`: time
- `onChange`: value가 변경됐을 때 호출되는 함수

<Canvas of={TimeRangePickerStories.ControlledTimeRangePicker} />

### Locale and Options

#### 1. Locale

- `locale`: BCP47 언어 태그를 포함하는 문자열

<Canvas of={TimeRangePickerStories.Locale} />

#### 2. Options

- `options`: time 속성

<Canvas of={TimeRangePickerStories.Options} />

### Restrict Time

#### 1. Min time

- `minTime`: 선택 가능한 최소 시간

<Canvas of={TimeRangePickerStories.MinTime} />

#### 2. Max time

- `maxTime`: 선택 가능한 최대 시간

<Canvas of={TimeRangePickerStories.MaxTime} />

#### 3. Disabled times

- `disabledTimes`: 비활성화하는 특정 시간 모음

<Canvas of={TimeRangePickerStories.DisabledTimes} />

### Time Steps

- `timeStep`: 두 time options 간 step
  - mode='preset'인 경우, `number` type (단위: s)
  - mode='manual'인 경우, `{hour: number, minute: number, second: number}` type

#### 1. Time step in preset mode

<Canvas of={TimeRangePickerStories.TimeStepInPresetMode} />

#### 2. Time step in manual mode

<Canvas of={TimeRangePickerStories.TimeStepInManualMode} />

### Readonly

- `readOnly`: true이면, 수정 불가

<Canvas of={TimeRangePickerStories.Readonly} />

### Disabled

- `disabled`: true이면, 비활성화됨

<Canvas of={TimeRangePickerStories.Disabled} />

### TimeRangePicker with form

<Canvas of={TimeRangePickerStories.TimeRangePickerWithForm} />

### Custom TimeRangeField

- `TimeRangeFieldProps`: TimeRangeField의 Props

<Canvas of={TimeRangePickerStories.CustomTimeRangeField} />

### Custom DigitalClock

- `renderDigitalClock`: digitalClockProps를 입력값으로 받아 mode에 따라 ManualDigitalClock 이나 PresetDigitalClock 컴포넌트를 렌더하는 함수

<Canvas of={TimeRangePickerStories.CustomDigitalClock} />

### Custom Popover

- `PopoverProps`: Popover 컴포넌트의 props

<Canvas of={TimeRangePickerStories.CustomPopover} />

### Multi-TimePicker

- `TimePicker` 컴포넌트를 두 개 사용해, 직접 시작 시간과 마지막 시간을 설정할 수도 있음

<Canvas of={TimeRangePickerStories.MultiTimePicker} />

## Props

<ArgTypes of={TimeRangePickerStories} sort="alpha" />
