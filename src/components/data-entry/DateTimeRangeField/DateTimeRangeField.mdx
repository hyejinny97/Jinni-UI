import { Meta, Canvas, ArgTypes, Stories, Markdown } from '@storybook/blocks';
import * as DateTimeRangeFieldStories from './DateTimeRangeField.stories';

<Meta of={DateTimeRangeFieldStories} />

# DateTimeRangeField

사용자가 선택한 날짜와 시간 범위가 나타나는 곳

## Import

```js
import { DateTimeRangeField } from '@/components/data-entry/DateTimeRangeField';
```

## Usage

### Basic DateTimeRangeField

- `placeholder`: placeholder
- `defaultValue`: 초기 date time (uncontrolled일 때만 적용 가능)

<Canvas of={DateTimeRangeFieldStories.BasicDateTimeRangeField} />

### Controlled DateTimeRangeField

- `value`를 통해 control 가능
- `value`: date time
- `onChange`: value가 변경됐을 때 호출되는 함수

<Canvas of={DateTimeRangeFieldStories.ControlledDateTimeRangeField} />

### Locale and Options

#### 1. Locale

- `locale`: BCP47 언어 태그를 포함하는 문자열

<Canvas of={DateTimeRangeFieldStories.Locale} />

#### 2. Options

- `options`: date time 속성

<Canvas of={DateTimeRangeFieldStories.Options} />

### DateTime Format

#### 1. Date format

- `dateFormat`: date format

  - `options` 대신 date tokens를 사용해 자유롭게 포맷 설정 가능
  - `format`과 `option`(DateOptions) prop 동시 사용 시, `option` prop의 값은 무시되고 `format` prop 값이 적용됨

<details>
  <summary>Date Tokens</summary>
  <Markdown>
    {`
| Token | Type | 특징 | 
| ----- | --------- | ---------------- | 
| YYYY | year | 'numeric' | 
| YY | year | '2-digit' | 
| MMMM | month | 'long' | 
| MMM | month | 'short' | 
| MM | month | '2-digit' | 
| M | month | 'numeric' | 
| DD | day | '2-digit' | 
| D | day | 'numeric' | 
`}
  </Markdown>
</details>

<Canvas of={DateTimeRangeFieldStories.DateFormat} />

#### 2. Time format

- `timeFormat`: time format

  - `options` 대신 time tokens를 사용해 자유롭게 포맷 설정 가능
  - `format`과 `option`(TimeOptions) prop 동시 사용 시, `option` prop의 값은 무시되고 `format` prop 값이 적용됨

<details>
  <summary>Time Tokens</summary>
  <Markdown>
    {`
| Token | Type      | 특징             |
| ----- | --------- | ---------------- |
| HH    | hour      | 'h23', '2-digit' |
| H     | hour      | 'h23', 'numeric' |
| hh    | hour      | 'h11', '2-digit' |
| h     | hour      | 'h11', 'numeric' |
| TT    | hour      | 'h24', '2-digit' |
| T     | hour      | 'h24', 'numeric' |
| tt    | hour      | 'h12', '2-digit' |
| t     | hour      | 'h12', 'numeric' |
| mm    | minute    | '2-digit'        |
| m     | minute    | 'numeric'        |
| ss    | second    | '2-digit'        |
| s     | second    | 'numeric'        |
| a     | dayPeriod | 'lowerCase'      |
| A     | dayPeriod | 'upperCase'      |
`}
  </Markdown>
</details>

<Canvas of={DateTimeRangeFieldStories.TimeFormat} />

### Restrict DateTime

#### 1. Min time

- `minTime`: 선택 가능한 최소 시간

<Canvas of={DateTimeRangeFieldStories.MinTime} />

#### 2. Max time

- `maxTime`: 선택 가능한 최대 시간

<Canvas of={DateTimeRangeFieldStories.MaxTime} />

#### 3. Disabled times

- `disabledTimes`: 비활성화하는 특정 시간 모음

<Canvas of={DateTimeRangeFieldStories.DisabledTimes} />

#### 4. Min date

- `minDate`: 선택 가능한 최소 날짜

<Canvas of={DateTimeRangeFieldStories.MinDate} />

#### 5. Max date

- `maxDate`: 선택 가능한 최대 날짜

<Canvas of={DateTimeRangeFieldStories.MaxDate} />

#### 6. Disabled dates

- `disabledDates`: 비활성화하는 특정 날짜 모음

<Canvas of={DateTimeRangeFieldStories.DisabledDates} />

### Time Steps

- `timeMode`: time 선택 방법
  - `'preset'`: '12:00', '12:30', '01:00' 처럼 미리 정의된 시간 목록에서 하나를 선택하는 방식
  - `'manual'`: 시(hour), 분(minute), 초(second)를 각각 선택하는 방식
- `timeStep`: 두 time options 간 step
  - timeMode='preset'인 경우, `number` type (단위: s)
  - timeMode='manual'인 경우, `{hour: number, minute: number, second: number}` type

#### 1. Time step in preset mode

<Canvas of={DateTimeRangeFieldStories.TimeStepInPresetMode} />

#### 2. Time step in manual mode

<Canvas of={DateTimeRangeFieldStories.TimeStepInManualMode} />

### Readonly

- `readOnly`: true이면, 수정 불가

<Canvas of={DateTimeRangeFieldStories.Readonly} />

### Disabled

<Canvas of={DateTimeRangeFieldStories.Disabled} />

### InputBase props

- `DateTimeRangeField` 컴포넌트는 내부적으로 `InputBase` 컴포넌트를 반환하므로, `InputBase` 의 모든 props 사용 가능

#### 1. Variants

<Canvas of={DateTimeRangeFieldStories.Variants} />

#### 2. Sizes

<Canvas of={DateTimeRangeFieldStories.Sizes} />

#### 3. Color

<Canvas of={DateTimeRangeFieldStories.Color} />

#### 4. Full Width

<Canvas of={DateTimeRangeFieldStories.FullWidth} />

#### 5. Disable Effect

<Canvas of={DateTimeRangeFieldStories.DisableEffect} />

### Adornments

- `startAdornment`: date time field 앞에 위치하는 부가 요소 (icon ,text 등)
- `endAdornment`: date time field 뒤에 위치하는 부가 요소 (icon ,text 등)

<Canvas of={DateTimeRangeFieldStories.Adornments} />

### Custom center icon

- `centerIcon`: 두 date time field 중앙에 위치한 아이콘

<Canvas of={DateTimeRangeFieldStories.CustomCenterIcon} />

### Multi-DateTimeField

- `DateTimeField` 컴포넌트를 두 개 사용해, 직접 시작 날짜/시간과 마지막 날짜/시간을 설정할 수도 있음

<Canvas of={DateTimeRangeFieldStories.MultiDateTimeField} />

## Props

<ArgTypes of={DateTimeRangeFieldStories} sort="alpha" />
