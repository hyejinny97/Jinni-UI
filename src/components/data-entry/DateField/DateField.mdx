import { Meta, Canvas, ArgTypes, Stories, Markdown } from '@storybook/blocks';
import * as DateFieldStories from './DateField.stories';

<Meta of={DateFieldStories} />

# DateField

사용자가 선택한 날짜가 나타나는 곳

## Import

```js
import { DateField } from '@/components/data-entry/DateField';
```

## Usage

### Basic DateField

- `placeholder`: placeholder
- `defaultValue`: 초기 date (uncontrolled일 때만 적용 가능)

<Canvas of={DateFieldStories.BasicDateField} />

### Controlled DateField

- `value`를 통해 control 가능
- `value`: date
- `onChange`: value가 변경됐을 때 호출되는 함수

<Canvas of={DateFieldStories.ControlledDateField} />

### Locale and Options

#### 1. Locale

- `locale`: BCP47 언어 태그를 포함하는 문자열

<Canvas of={DateFieldStories.Locale} />

#### 2. Options

- `options`: date 속성

<Canvas of={DateFieldStories.Options} />

### Date Format

- `format`: date format

  - `options` 대신 date tokens를 사용해 자유롭게 포맷 설정 가능
  - `format`과 `option` prop 동시 사용 시, `option` prop의 값은 무시되고 `format` prop 값이 적용됨

<details>
  <summary>Date Tokens</summary>
  <Markdown>
    {`
| Token | Type | 특징 | 
| ----- | --------- | ---------------- | 
| YYYY | year | 'numeric' | 
| YY | year | '2-digit' | 
| MMMM | month | 'long' | 
| MMM | month | 'short' | 
| MM | month | '2-digit' | 
| M | month | 'numeric' | 
| DD | day | '2-digit' | 
| D | day | 'numeric' | 
`}
  </Markdown>
</details>

<Canvas of={DateFieldStories.DateFormat} />

### Restrict Date

#### 1. Min date

- `minDate`: 선택 가능한 최소 날짜

<Canvas of={DateFieldStories.MinDate} />

#### 2. Max date

- `maxDate`: 선택 가능한 최대 날짜

<Canvas of={DateFieldStories.MaxDate} />

#### 3. Disabled dates

- `disabledDates`: 비활성화하는 특정 날짜 모음

<Canvas of={DateFieldStories.DisabledDates} />

### Detect validation error status

- `onErrorStatus`: validation error status가 변경됐을 때 호출되는 함수

<Canvas of={DateFieldStories.DetectValidationErrorStatus} />

### Readonly

- `readOnly`: true이면, 수정 불가

<Canvas of={DateFieldStories.Readonly} />

### InputBase props

- `DateField` 컴포넌트는 내부적으로 `InputBase` 컴포넌트를 반환하므로, `InputBase` 의 모든 props 사용 가능

#### 1. Variants

<Canvas of={DateFieldStories.Variants} />

#### 2. Sizes

<Canvas of={DateFieldStories.Sizes} />

#### 3. Disabled

<Canvas of={DateFieldStories.Disabled} />

#### 4. Color

<Canvas of={DateFieldStories.Color} />

#### 5. Full Width

<Canvas of={DateFieldStories.FullWidth} />

#### 6. Adornments

<Canvas of={DateFieldStories.Adornments} />

#### 7. Disable Effect

<Canvas of={DateFieldStories.DisableEffect} />

## Props

<ArgTypes of={DateFieldStories} sort="alpha" />
