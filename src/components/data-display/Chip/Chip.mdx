import { Meta, Canvas, ArgTypes, Stories } from '@storybook/blocks';
import * as ChipStories from './Chip.stories';

<Meta of={ChipStories} />

# Chip

(사용자의 관심을 끌고 한눈에 알기 쉽게) 정보 제공, 콘텐츠 선택, 액션 유발하기 위해 사용됨

## Import

```js
import { Chip } from '@/components/data-display/Chip';
```

## Usage

### Basic Chip

- `children`: chip content

<Canvas of={ChipStories.BasicChip} />

### Chip Adornments

- `startAdornment`: content 앞에 위치하는 부가 요소 (icon, avatar 등)
- `endAdornment`: content 뒤에 위치하는 부가 요소 (icon, avatar 등)

#### 1. Avatar chip

<Canvas of={ChipStories.AvatarChip} />

#### 2. Icon chip

<Canvas of={ChipStories.IconChip} />

### Chip Actions

#### 1. Clickable chip

<Canvas of={ChipStories.ClickableChip} />

#### 2. Deletable chip

<Canvas of={ChipStories.DeletableChip} />

#### 3. Clickable and deletable chip

<Canvas of={ChipStories.ClickableDeletableChip} />

#### 4. Link chip

<Canvas of={ChipStories.LinkChip} />

### Chip Variant

- `variant`: chip 종류

<Canvas of={ChipStories.ChipVariant} />

### Chip Shape

- `shape`: chip 모양

<Canvas of={ChipStories.ChipShape} />

### Color

- `color`: chip 색상

<Canvas of={ChipStories.Color} />

### Size

- `size`: chip 크기

<Canvas of={ChipStories.Size} />

### Customization

<Canvas of={ChipStories.Customization} />

## Props

<ArgTypes of={ChipStories} sort="alpha" />
