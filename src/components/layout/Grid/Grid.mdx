import { Meta, Canvas, ArgTypes, Stories } from '@storybook/blocks';
import * as GridStories from './Grid.stories';

<Meta of={GridStories} />

# Grid

그리드 컨테이너

## Import

```js
import { Grid } from '@/components/layout/Grid';
```

## Usage

### Basic Grid

#### 1. Basic

- `children`: grid items
- `rows`: 동일한 크기의 row 갯수
- `columns`: 동일한 크기의 column 갯수

<Canvas of={GridStories.BasicGrid} />

#### 2. Responsive

<Canvas of={GridStories.ResponsiveGrid} />

### Spacing

#### 1. Spacing

- `spacing`: row-gap과 column-gap

<Canvas of={GridStories.Spacing} />

#### 2. Row/Column spacing

- `rowSpacing`: row-gap
- `columnSpacing`: column-gap

<Canvas of={GridStories.RowColumnSpacing} />

#### 3. Responsive

<Canvas of={GridStories.ResponsiveSpacing} />

### Grid Item Area

#### 1. Span

<Canvas of={GridStories.Span} />

#### 2. Row start/end line

<Canvas of={GridStories.RowStartEnd} />

#### 3. Column start/end line

<Canvas of={GridStories.ColumnStartEnd} />

### Grid Flow

- `flow`: grid items가 auto placement 되는 방식 결정

<Canvas of={GridStories.GridFlow} />

### Nested Grid

<Canvas of={GridStories.NestedGrid} />

### Customization

#### 1) grid-template-rows, grid-template-columns

- `style` prop의 grid-template-rows, grid-template-columns를 통해 grid items의 명시적인 크기 지정 가능
- 단, n개의 동일한 row/column을 가진 grid items를 생성하려고 할 경우 `rows`, `columns` prop을 사용하면 됨

<Canvas of={GridStories.GridTemplateCustomization} />

#### 2) grid-auto-rows, grid-auto-columns

- `style` prop의 grid-auto-rows, grid-auto-columns를 통해 grid items의 암시적인 크기 지정 가능
- 명시적으로 정의된 row/column을 벗어날 경우, 자동으로 row/column 크기를 설정하게 됨

<Canvas of={GridStories.GridAuto} />

#### 3) grid-template-areas, grid-area

- `style` prop의 grid-template-areas, grid-area를 통해 grid area에 이름을 지어 구조화 가능

<Canvas of={GridStories.GridTemplateArea} />

#### 4) justify-items, align-items

- `style` prop의 justify-items, align-items를 통해 grid item 내 contents의 상하좌우 정렬 가능

<Canvas of={GridStories.Alignment} />

## Props

<ArgTypes of={GridStories} sort="alpha" />
